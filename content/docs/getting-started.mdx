---
title: Getting Started
description: Learn how to use the Z.AI Usage Dashboard
icon: Rocket
---

This guide will help you get started with the Z.AI Usage Dashboard.

## Prerequisites

Before you begin, make sure you have:

- An active Z.AI Coding Plan subscription
- Your API key from the Z.AI platform

<Callout type="info">
Don't have a subscription yet? Visit [Z.AI Pricing](https://z.ai/pricing) to choose a plan that fits your needs.
</Callout>

## Getting Your API Key

<Steps>
<Step>

### Visit Z.AI Platform

Go to [Z.AI Platform](https://z.ai/manage-apikey/apikey-list) and log in to your account.

</Step>

<Step>

### Navigate to API Keys

Click on **API Keys** in the sidebar menu.

</Step>

<Step>

### Create or Copy Key

Create a new API key or copy an existing one. Your key format should be:

```
xxxxxxxxxxxxxxxxxxxxxxxxxxxxxxxx.xxxxxxxxxxxxxxxx
```

</Step>
</Steps>

<Callout type="warn">
Keep your API key secure! Never share it publicly or commit it to version control.
</Callout>

## Using the Dashboard

<Steps>
<Step>

### Set a Master Password

On your first visit the dashboard asks you to choose a master password. Every API key you add is encrypted with it before anything is written to disk, and the password itself is never stored — you enter it once per browser session.

</Step>

<Step>

### Add Your Accounts

Click **Add account**, give it a name, and paste the API key (or use the clipboard button). Repeat for each Z.AI account you want to watch.

</Step>

<Step>

### Analyze Your Data

Every account is fetched in parallel. A summary table at the top compares them side by side and totals the last 24 hours; below it each account gets its own section with quota cards and charts. Pick an auto-refresh interval in the header, or refresh a single account from its section.

</Step>
</Steps>

## Understanding the Metrics

<Tabs items={['Token Usage', 'MCP Usage', 'Model Stats']}>
<Tab value="Token Usage">

### Token Usage (5 Hour)

This shows your token consumption within the current 5-hour cycle.

- **Current Usage**: Tokens consumed so far
- **Total Quota**: Your plan's token limit
- **Remaining**: Tokens still available
- **Reset Time**: When the quota resets

</Tab>
<Tab value="MCP Usage">

### MCP Usage (1 Month)

Monthly usage of MCP (Model Call Points) if applicable to your plan.

- Tracks overall API call volume
- Resets at the start of each billing cycle

</Tab>
<Tab value="Model Stats">

### Model Usage Statistics

- **Input Tokens**: Tokens sent to the model
- **Output Tokens**: Tokens generated by the model
- **Total Tokens**: Combined input and output
- **Request Count**: Number of API calls made

</Tab>
</Tabs>

## Tips for Efficient Usage

<Cards>
<Card icon={<Clock />} title="Monitor Regularly">
Check your usage frequently to avoid hitting limits unexpectedly.
</Card>

<Card icon={<Zap />} title="Optimize Prompts">
Use concise prompts to reduce token consumption without sacrificing quality.
</Card>

<Card icon={<Settings />} title="Choose Right Model">
Use lighter models for simpler tasks to save tokens for complex work.
</Card>
</Cards>
